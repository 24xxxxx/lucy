name: Test Cron
on:
  schedule:
    - cron: '*/5 * * * *'   # هر ۵ دقیقه یک بار
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Cron is working at $(date)"
